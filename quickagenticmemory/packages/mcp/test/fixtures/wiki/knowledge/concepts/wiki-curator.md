---
title: Wiki Curator
tags: [agent, wiki, maintenance]
---

# Wiki Curator

The curator can propose a Markdown update for human review. Proposal support is absent from the
MCP tool list by default and requires an explicitly configured proposal service. It never edits
the graph projection directly.
